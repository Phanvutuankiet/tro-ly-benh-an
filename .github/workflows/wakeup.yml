name: Wake Up Streamlit App

on:
  schedule:
    # Chạy sau mỗi 15 phút
    - cron: '*/15 * * * *'

jobs:
  wake-up:
    runs-on: ubuntu-latest
    steps:
      - name: Send request to app
        run: curl -L --fail "https://tro-ly-benh-an-askooddzgsysf2vk5cwui5.streamlit.app" || exit 0
